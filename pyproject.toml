[project]
name = "maverx-training-builder"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.29.0",
  "python-pptx>=0.6.21",
  "python-docx>=1.1.0",
  "requests>=2.31.0",
  "python-dotenv>=1.0.0"
]

[tool.vercel]
entrypoint = "api:app"
